import { EventSender } from '../types';
export declare function Refreshable<S extends EventSender>(Sender: S): {
    new (...args: any[]): {
        /**
         * Triggers a refresh of the chart or dashboard (if it has been embedded).
         *
         * @returns a promise that resolves once the chart or dashboard updated its data
         */
        refresh(): Promise<void>;
        _send(msgName: string, ...payload: unknown[]): Promise<[unknown]>;
    };
} & S;
